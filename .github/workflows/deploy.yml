name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  # Cho phép chạy tay từ tab Actions
  workflow_dispatch:

# Quyền tối thiểu để deploy Pages. `id-token` cần cho actions/deploy-pages.
permissions:
  contents: read
  pages: write
  id-token: write

# Đẩy liên tiếp thì huỷ lượt đang chạy, nhưng KHÔNG huỷ lượt đang deploy dở:
# huỷ giữa lúc deploy có thể để lại site ở trạng thái nửa vời.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      # `enablement: true` chuyển Pages của repo sang chế độ "GitHub Actions".
      #
      # Cần vì mặc định repo đang ở chế độ "Deploy from a branch" (main, gốc), và
      # ở chế độ đó Pages phục vụ MÃ NGUỒN chưa build: index.html trỏ vào
      # ./src/main.ts, mà TypeScript thì trình duyệt không chạy được. Bước này
      # chạy TRƯỚC build để nếu không đủ quyền thì workflow đỏ ngay, thay vì build
      # xong rồi mới chết ở bước deploy.
      - uses: actions/configure-pages@v5
        with:
          enablement: true

      # `npm ci` chứ không `npm install`: cài đúng theo package-lock nên bản
      # deploy dùng chính những phiên bản đã chạy test ở máy
      - run: npm ci

      # Test TRƯỚC khi build. Deploy một bản hỏng lên URL công khai rồi mới phát
      # hiện thì tệ hơn nhiều so với để workflow đỏ.
      - run: npm test

      # `npm run build` đã chạy `tsc --noEmit` bên trong, nên không cần bước
      # typecheck riêng
      - run: npm run build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
